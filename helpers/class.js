//Stores the Note class used in storing notes to the 'db.json' file

class Note {
    constructor(title, text, id) {
        title = this.title;
        text - this.text;
        id = this.id;
    }
}

//Exports 'Note' class
module.exports = (Note)